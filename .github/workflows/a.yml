name: Build deb package

on:
  push:
    branches:
      - branchAutoPackage
  pull_request:
    branches:
      - branchAutoPackage

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: AutoPackage build deb
      run: |
        autoreconf --install
        ./configure --prefix=/
        make install DESTDIR=$(pwd)
        dpkg-deb --build deb
